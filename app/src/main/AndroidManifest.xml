<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ltdd_chatfirebase">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LTDD_ChatFirebase">
        <activity android:name=".JohanActivity">

        </activity>
        <activity android:name=".MessageListActivity" />
        <activity
            android:name=".MainChatActivity"
            android:label="@string/title_activity_main_chat"
            android:theme="@style/Theme.LTDD_ChatFirebase.NoActionBar">


        </activity>
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/Theme.LTDD_ChatFirebase1" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.LTDD_ChatFirebase1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>